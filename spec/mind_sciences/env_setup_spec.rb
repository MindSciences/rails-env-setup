RSpec.describe MindSciences::EnvSetup do
  it "has a version number" do
    expect(MindSciences::EnvSetup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
